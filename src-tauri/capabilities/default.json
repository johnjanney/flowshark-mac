{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The smallest set of capabilities FlowShark needs. Anything not listed here is unavailable to the front end. The Open and Save panels and the reveal-in-Finder action are deliberately absent: they are presented from Rust so the web layer receives a capability for the file the user chose rather than a pathname it could have chosen itself.",
  "windows": [
    "main",
    "main-*"
  ],
  "permissions": [
    "core:default",
    "core:menu:default",
    "core:window:allow-set-title",
    "core:window:allow-close",
    "core:window:allow-set-focus",
    "core:window:allow-start-dragging",
    "core:event:default",
    "dialog:allow-message",
    "dialog:allow-ask",
    "dialog:allow-confirm",
    "clipboard-manager:allow-read-text",
    "clipboard-manager:allow-write-text",
    "clipboard-manager:allow-write-image",
    "clipboard-manager:allow-read-image"
  ]
}
